# Agent 阶段 5：数字衣橱与复杂场景

- Java 是衣橱单品、衣橱版本和反馈事件的唯一事实源；反馈仅保存引用，不改写商品事实。
- Python 读取带版本的衣橱快照，只在 Checkpoint 中保存版本、摘要和单品引用；规划时优先复用已有单品，只检索缺失品类。
- 前端通过 `wardrobe_plan` SSE 展示方案，支持锁定、删除和替换单件；局部重算先校验衣橱版本，版本变化返回 409。
- 复杂任务硬约束：套数与预算；场景、日期、天气作为规划上下文。无法满足时返回降级方案，不执行购物车写入。
