# 下单前价格校验

购物车只保存加入时的商品快照，不能作为最终成交价。下单时系统会先并发获取每个已选商品的权威目录快照，再按责任链校验商品一致性、价格和库存事实。

价格发生变化时，不创建订单；系统会更新购物车中的名称、图片和价格，并返回 `409`，用户确认新价格后可使用同一个幂等键重新下单。商品失效或库存不足同样返回 `409`，但不会修改购物车。

目录请求使用有界线程池，默认并发数为 `4`，通过 `CHECKOUT_CATALOG_PARALLELISM` 调整（范围 `1-16`）。
